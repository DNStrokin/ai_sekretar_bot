# Взаимодействие бота с языковыми моделями
## Проект: Telegram-бот «Личный секретарь»

---

## 1. Назначение документа

Данный документ описывает **архитектуру и логику взаимодействия Telegram-бота с языковыми моделями (LLM)** и моделью распознавания речи (STT).

Цель — обеспечить:
- предсказуемое поведение бота;
- экономию токенов;
- разделение ответственности между этапами обработки;
- масштабируемость при росте количества тем и пользователей.

Документ предназначен для разработчиков, впервые знакомящихся с проектом.

---

## 2. Общая архитектура взаимодействия

Взаимодействие с AI строится **в несколько независимых шагов**, а не одним универсальным запросом.

Используется принцип:

> *Сначала понять — потом оформить.*

### Основные этапы:
1. (опционально) Распознавание речи → текст (STT)
2. Классификация заметки по темам
3. Форматирование заметки по правилам выбранной темы

Каждый этап — отдельный вызов модели.

---

## 3. Распознавание речи (STT)

### 3.1 Когда используется
- Пользователь отправил голосовое сообщение

### 3.2 Входные данные
- audio/voice файл из Telegram
- язык: русский

### 3.3 Выходные данные
- plain text (строка)

### 3.4 Требования
- без таймкодов
- без служебных пометок
- без попыток форматирования

### 3.5 Пример результата

```
Купить новый микрофон для записи подкаста и сравнить модели до 15000 рублей
```

Полученный текст передаётся в дальнейшие этапы как обычная текстовая заметка.

---

## 4. Этап 1 — Классификация заметки по темам

### 4.1 Цель этапа

- определить, к какой теме относится информация;
- предложить пользователю корректную тему;
- выявить ситуацию, когда **подходящей темы не существует**.

### 4.2 Входные данные

В модель передаётся:

1. **Текст заметки**
   - для текста/голоса — нормализованный текст;
   - для ссылки — title + meta description;
   - для файла — caption + имя файла + тип.

2. **Список тем** (динамический контекст):

```
[
  {
    "topic_id": 123,
    "title": "Идеи",
    "description": "Мысли, идеи, гипотезы для проектов и бизнеса"
  },
  {
    "topic_id": 456,
    "title": "Покупки",
    "description": "Товары и услуги, которые нужно купить или изучить"
  }
]
```

### 4.3 Системный промпт (пример)

```
Ты AI-помощник, который классифицирует заметки по темам.
Язык — русский.
Ты не выдумываешь факты.
Ты возвращаешь ответ строго в формате JSON.
```

### 4.4 Пользовательский промпт (пример)

```
Заметка:
"Купить новый микрофон для записи подкаста"

Доступные темы:
1) Идеи — Мысли, идеи, гипотезы для проектов и бизнеса
2) Покупки — Товары и услуги, которые нужно купить или изучить

Определи наиболее подходящую тему.
Верни JSON.
```

### 4.5 Формат ответа (строго)

```
{
  "suggested_topic_id": 456,
  "top_topics": [
    { "topic_id": 456, "confidence": 0.92 },
    { "topic_id": 123, "confidence": 0.21 }
  ],
  "need_new_topic": false
}
```

### 4.6 Правила

- confidence — относительный показатель, не абсолютная метрика
- если ни одна тема не подходит:
  - `need_new_topic = true`
- модель **не придумывает новые темы**, только сигнализирует

---

## 5. Этап 2 — Форматирование заметки

### 5.1 Когда вызывается

- только после того, как пользователь подтвердил тему

### 5.2 Входные данные

1. Исходный текст заметки
2. Выбранная тема:
   - title
   - description
3. Правила форматирования темы (`format_policy_text`)

### 5.3 Системный промпт (пример)

```
Ты AI-секретарь.
Ты оформляешь заметки аккуратно и структурированно.
Язык — русский.
Ты строго следуешь правилам форматирования темы.
```

### 5.4 Пользовательский промпт (пример)

```
Исходный текст:
"Купить новый микрофон для записи подкаста"

Тема:
Покупки — Товары и услуги, которые нужно купить или изучить

Правила оформления:
- Заголовок обязателен
- Список до 5 пунктов
- Добавь блок "Следующие шаги"
- Теги в конце, формат #слитно

Сформируй итоговую заметку.
```

### 5.5 Формат ответа

Обычный текст (Markdown совместимый), например:

```
📝 Микрофон для подкаста

Нужно подобрать микрофон для записи подкаста в домашней студии.

Основное:
— рассмотреть модели до 15 000 ₽
— сравнить USB и XLR варианты
— проверить отзывы

Следующие шаги:
— составить список моделей
— посмотреть обзоры

🏷 #покупки #подкаст #оборудование
```

---

## 6. Генерация тегов

Теги:
- генерируются на этапе форматирования
- добавляются в конец заметки
- формат: #слитно
- 3–8 штук
- на русском языке

---

## 7. Работа со ссылками (оптимизация токенов)

Для ссылок в LLM передаются **только**:
- title страницы
- meta description
- URL

Пример входа:

```
Заголовок: Как выбрать микрофон для подкаста
Описание: Краткий гайд по выбору микрофона для начинающих подкастеров
Ссылка: https://example.com/microphones
```

Модель **не получает HTML страницы**.

---

## 8. Обработка ошибок и fallback-логика

### 8.1 Ошибка классификации
- если JSON некорректный → повтор запроса
- если повтор неудачен → предложить пользователю выбор темы вручную

### 8.2 Ошибка форматирования
- повторить запрос с упрощённым промптом
- fallback на формат по умолчанию

### 8.3 Отказ AI
- бот сообщает пользователю: «Не удалось обработать заметку, попробуй позже»

---

## 9. Масштабирование

При росте количества тем:
- ограничивать длину description
- предварительно отбирать релевантные темы (keyword/embedding search)
- в LLM отправлять только top-N тем

---

## 10. Итоговая схема

```
[Вход]
   ↓
[STT, если нужно]
   ↓
[Классификация темы]
   ↓ (подтверждение пользователем)
[Форматирование заметки]
   ↓
[Публикация в Telegram]
```

---

## 11. Ключевой принцип

> Модель не принимает решений за пользователя.
> Она предлагает, а пользователь подтверждает.

Это делает поведение бота надёжным, предсказуемым и удобным для долгосрочного использования.

